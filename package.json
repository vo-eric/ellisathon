{
  "name": "wikirun",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n worker,client -c blue,magenta \"wrangler dev\" \"npm run dev --prefix client\"",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "concurrently": "^9.1.2",
    "typescript": "^5.9.3",
    "wrangler": "^4"
  }
}
